# ── OS / editor junk ──────────────────────────────────────────────
.DS_Store
*.swp
.ipynb_checkpoints/

# ── Python ────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
env/

# ── Personal course materials (kept locally; not project deliverables) ──
# Lecture slides + lab notebooks are studied on-demand and are course IP,
# not part of what we hand in. They stay on disk but out of the repo.
הרצאות כיתה/
מעבדות/
סילבוס סימולציה.pdf
presentation guidelines.docx

# ── Presentation deck (edited out-of-band, shared via WhatsApp) ──
# The slide deck and its exports are collaborated on separately and are not a
# repo deliverable, so they stay out of the shared repo.
*Deck*.html
*Deck*_files/
*Deck*.pptx
*Deck*.pdf
